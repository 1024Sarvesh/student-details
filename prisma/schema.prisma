generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model studentReg{
    id Int @id @default(autoincrement())
    name String 
    fatherName String
    mobileNo Int @unique
    Address String 
}